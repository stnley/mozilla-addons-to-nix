image: nixos/latest
repositories:
  nixpkgs: https://nixos.org/channels/nixpkgs-unstable
sources:
  - https://git.sr.ht/~rycee/mozilla-addons-to-nix
tasks:
  - build: |
      cd mozilla-addons-to-nix
      nix-build
